#!/usr/bin/env perl
use strict;

$, = ' ';

my @list = ('Аа', 'Ab', 'Cc', 2, 3, '1', 'Zя');

print sort {(($a =~ /^[а-яА-ЯёЁ]+$/) && ($b !=~ /^[а-яА-ЯёЁ]+$/)) ? (-1) : (($a !=~ /^[а-яА-ЯёЁ]+$/) && ($b =~ /^[а-яА-ЯёЁ]+$/)) ? (1) : ($a cmp $b)} @list;
